server:
  port: 18080
  servlet:
    context-path: /xxl-job-admin
spring:
  profiles:
    active: dev
  application:
    name: structure-job
  main:
    allow-circular-references: true
#    allow-bean-definition-overriding: true
management:
  endpoint:
    health:
      show-details: always
  server:
    port: 7779
  health:
    mail:
      enabled: false